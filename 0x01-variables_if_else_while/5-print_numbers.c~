#include <stdio.h>
/**
 * main - this program will print some letters of alphabet
 *
 * Return: Always (Success)
 */
int main(void)
{
char c;
for (c = 'a'; c <= 'z'; c++)
{
if (c != 'é' && c != 'q')
{
putchar(c);
}
}
putchar('\n');
return (0);
}
